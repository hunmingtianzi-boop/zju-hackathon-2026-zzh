# KIA — Knowledge Integration Agent
# Python 依赖 (Python 3.8+)，严格锁定版本确保可复现

# 文件解析
PyMuPDF==1.24.0          # PDF 解析
python-docx==1.1.0       # Word .docx 解析
openpyxl==3.1.2          # Excel .xlsx 解析

# 自然语言处理
jieba==0.42.1            # 中文分词
rank-bm25==0.2.2         # BM25 关键词检索

# 向量嵌入与检索
sentence-transformers==2.7.0  # 语义向量模型
faiss-cpu==1.7.4         # 向量相似度检索 (CPU版)

# 图计算
networkx==3.2.1          # 知识图谱存储与图算法

# 科学计算
numpy==1.26.4            # 矩阵运算

# 进度条
tqdm==4.66.2             # 索引进度显示
